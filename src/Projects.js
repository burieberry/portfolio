import React from 'react';

import './projects.css';

const Projects = ({ title }) => {
  return (
    <div>
      <h2 className='subhead'>{ title }</h2>
      <ul>
        {/* projects TK */}
      </ul>
    </div>
  );
}

export default Projects;
